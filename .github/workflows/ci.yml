name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  quality:
    name: Quality (Ubuntu / Node ${{ matrix.node }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        node: [22, "lts/*"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      # The parser and deep-call-chain suites build very large inputs, and V8
      # coverage instrumentation on top of that pushes a single worker past the
      # default ~4GB heap ceiling ("Fatal process out of memory: Zone") on
      # Node 24. The 16GB runner is not the limit — the per-process cap is.
      - run: npm run coverage
        env:
          NODE_OPTIONS: --max-old-space-size=6144
      - run: npm run build
      - run: npm run smoke
      - run: npm run quality:check
      - run: npm run quality:claims
      - run: npm audit --omit=dev --audit-level=high
      - run: npm pack --dry-run

  native:
    name: Native + packed demo (${{ matrix.os }} / Node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: [22, "lts/*"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - name: Load native dependencies
        run: node -e "require('better-sqlite3'); require('@lancedb/lancedb')"
      - run: npm run build
      - run: npm run demo:packed
